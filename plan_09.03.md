## План улучшений Angular-проекта (09.03.2026)

### 1. Навигация и структура компонентов

1.1. **Включить нормальный routing**
- В `App`:
  - В шаблоне `app.html` удалить прямые селекторы `<my-app>`, `<second-app>`, `<my-form>`.
  - Добавить `<router-outlet></router-outlet>`.
- Убедиться, что `AppRoutingModule` импортирован в `AppModule` (уже импортирован).

1.2. **Разнести список и форму по страницам**
- Оставить маршруты:
  - `/users` → компонент `Secondcomponent`.
  - `/add_users` → компонент `UserForm`.
- В шаблоне навигации (например, в `app.html`) добавить ссылки:
  - Ссылка на список пользователей (`routerLink="/users"`).
  - Ссылка на форму добавления (`routerLink="/add_users"`).

1.3. **Определиться с ролью компонента `Mycomponent`**
- Вариант A: использовать как домашнюю страницу (`path: ''`), перенести в маршрутизатор.
- Вариант B: если это учебный/лишний компонент — удалить его и связанные файлы.

---

### 2. Архитектура модулей и сервисов

2.1. **Перевести `UserService` на `providedIn: 'root'`**
- В `user-service.ts`:
  - Изменить декоратор на `@Injectable({ providedIn: 'root' })`.
  - Удалить `UserService` из `providers` массива `AppModule`.

2.2. **Подготовить структуру feature-модуля для пользователей (на будущее)**
- Создать папку `src/app/users/` и перенести туда:
  - `User` модель.
  - Компоненты списка и формы.
  - `UserService`.
- Создать `UsersModule` с декларациями и собственными маршрутами (lazy-loading).
- Подключить `UsersModule` через lazy route в `AppRoutingModule` (опционально, если проект будет расти).

---

### 3. Работа с API и конфигурацией окружений

3.1. **Вынести URL backend в конфигурацию окружения**
- Создать/использовать файлы окружений (`environment.ts`, `environment.development.ts`, `environment.production.ts`).
- Добавить туда:
  - `apiBaseUrl: 'http://localhost:8080'` (для разработки).
- В `UserService`:
  - Заменить жёстко закодированные строки:
    - `this.usersUrl = environment.apiBaseUrl + '/users';`
    - `this.addUrl  = environment.apiBaseUrl + '/add_users';`

3.2. **Подготовить возможность смены окружения**
- Убедиться, что в `angular.json` правильно настроены file replacements для `environment` файлов.
- Задокументировать в `README.md`, как менять `apiBaseUrl` для других серверов.

---

### 4. Качество кода и обработка ошибок

4.1. **Обработка ошибок в `UserService`**
- Добавить обработку ошибок для `findAll()` и `save()` (например, через `catchError`).
- Пробрасывать информативные ошибки в компоненты или возвращать безопасные значения.

4.2. **Обработка ошибок и статусов в компонентах**
- В `Secondcomponent`:
  - Добавить поля `loading`, `error`.
  - При загрузке выставлять `loading = true`, по завершении — `false`.
  - При ошибке сохранять сообщение в `error` и отображать в шаблоне.
- В `UserForm`:
  - Аналогично обрабатывать ошибки сохранения (`save()`):
    - Показать сообщение об ошибке пользователю.
    - Не переходить на `/users` при неуспехе.

4.3. **Убрать неиспользуемые зависимости**
- В `Secondcomponent`:
  - Удалить неиспользуемую зависимость `HttpClient` из конструктора, оставить только `UserService`.

4.4. **Ужесточить типизацию модели `User`**
- Рассмотреть переход с класса на интерфейс:
  - `export interface User { id: string; name: string; email: string; }`
- Минимизировать использование опциональных полей, если по бизнес-логике они обязательны.

---

### 5. Формы и UX

5.1. **Добавить валидацию формы пользователя**
- В `UserForm` и `user-form.html`:
  - Добавить атрибуты `required`, `email` и т.п.
  - Показать сообщения об ошибках под полями при некорректном вводе.
- При желании перейти на Reactive Forms для более сложной валидации.

5.2. **Улучшить UX после создания пользователя**
- После успешного сохранения:
  - Переходить на `/users`.
  - Показывать уведомление «Пользователь успешно создан» (toast/alert/баннер).
- В списке пользователей можно подсветить только что добавленного пользователя (опционально).

5.3. **Добавить простую навигацию**
- В шапке/основном шаблоне:
  - Кнопки/ссылки:
    - «Список пользователей»
    - «Добавить пользователя»
- Использовать `routerLink` и классы активного маршрута для подсветки текущей страницы.

---

### 6. Инфраструктура и сборка

6.1. **Папка assets / public**
- Проверить фактическое использование папки `public`:
  - Если статика лежит в `public`, оставить конфиг как есть.
  - Если статику планируется держать по стандарту Angular, сменить `input` на `src/assets` и перенести файлы туда.

6.2. **Линтер и форматирование**
- Добавить/настроить ESLint для Angular-проекта:
  - Базовый набор правил по стилю и лучшим практикам.
- Настроить форматирование (Prettier или встроенный форматер):
  - Единый стиль отступов, кавычек, порядка импортов и т.п.

6.3. **Тесты**
- Актуализировать существующие `*.spec.ts` файлы:
  - Для `UserService`: тестировать запросы и обработку ошибок с помощью `HttpTestingController`.
  - Для `Secondcomponent`: проверять, что список пользователей отображается по данным сервиса.
  - Для `UserForm`: проверять, что отправка формы вызывает `save()` и навигацию, а при ошибке отображается сообщение.

---

### 7. Приоритизация шагов

**Первая очередь (минимальные, но заметные улучшения):**
1. Настроить routing через `<router-outlet>` и убрать прямой рендер компонентов в `app.html`.
2. Вынести URL backend в конфигурацию окружениий.
3. Перевести `UserService` на `providedIn: 'root'` и убрать лишние зависимости (`HttpClient` в `Secondcomponent`).

**Вторая очередь (качество и UX):**
4. Добавить обработку ошибок и статусы загрузки в компоненты.
5. Добавить валидацию формы и простые UX-улучшения (уведомления, навигация).

**Третья очередь (масштабируемость и поддерживаемость):**
6. Подготовить/вынести users-функциональность в отдельный feature-модуль (при росте проекта).
7. Настроить линтер, форматирование и улучшить тестовое покрытие.

